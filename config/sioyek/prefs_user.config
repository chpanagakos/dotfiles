# ==========================================
# GENERAL BEHAVIOR
# ==========================================
# Open new files in a new window instance
should_launch_new_window    1

# Default default launch mode (0 = Standard PDF view)
startup_commands            toggle_statusbar

# ==========================================
# SEMANTIC HIGHLIGHTS (The "Sioyek Way")
# ==========================================
# Usage: Select text -> Press 'h' -> Press the letter code below.

# --- TYPE 'y': Standard Highlight (Yellow) ---
# The classic highlighter pen.
highlight_color_y           1.0 1.0 0.0
highlight_description_y     Standard Highlight

# --- TYPE 'i': Important / Critical (Red) ---
# Use for key arguments or "must read" sections.
highlight_color_i           1.0 0.4 0.4
highlight_description_i     Important

# --- TYPE 'd': Definition / Term (Green) ---
# Use when a new term is defined or a formula is introduced.
highlight_color_d           0.4 1.0 0.4
highlight_description_d     Definition

# --- TYPE 'q': Question / Confusion (Blue) ---
# Use for parts you don't understand or want to research later.
highlight_color_q           0.4 0.4 1.0
highlight_description_q     Question / Check Later

# --- TYPE 'r': Reference / Citation (Purple) ---
# Use for citations you need to look up in the bibliography.
highlight_color_r           0.7 0.4 1.0
highlight_description_r     Reference

# --- TYPE 'x': Strikethrough / Delete (Grey) ---
# Use to mark text as "deleted" or irrelevant (Workaround).
highlight_color_x           0.5 0.5 0.5
highlight_description_x     Strikethrough / Ignore

# ==========================================
# UI TWEAKS
# ==========================================
# Clean font for the status bar
ui_font                     Terminus
font_size                   12
